// =========================================================
// Глобальні модалки (поза панелями).
// Модалку налаштувань скрині перенесено в ChestsPage (React-стан),
// тож наразі порожньо — компонент лишається точкою розширення.
// =========================================================

export default function GlobalModals() {
  return null;
}
